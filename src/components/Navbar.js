import React from "react";


const Navbar = () => (
    <div className="container">
    <nav className="navbar navbar bg-primary">
    <a  className="bg-light" href="/">Re-Try</a>
  </nav>
  </div>
);

export default Navbar;
